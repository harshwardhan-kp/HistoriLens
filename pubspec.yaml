name: historilens
description: "Explore historical events through multiple cultural, religious, and intellectual perspectives."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.4
  http: ^1.2.2
  flutter_dotenv: ^5.2.1
  shared_preferences: ^2.3.3
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  shimmer: ^3.0.0
  smooth_page_indicator: ^1.2.0+3
  go_router: ^14.8.1
  provider: ^6.1.2
  flutter_markdown: ^0.7.5+2
  share_plus: ^10.1.4
  phosphor_flutter: ^2.1.0
  screenshot: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
